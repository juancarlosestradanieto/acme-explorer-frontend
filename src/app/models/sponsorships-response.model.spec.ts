import { SponsorshipsResponse } from './sponsorships-response.model';

describe('SponsorshipsResponse', () => {
  it('should create an instance', () => {
    expect(new SponsorshipsResponse(null)).toBeTruthy();
  });
});
